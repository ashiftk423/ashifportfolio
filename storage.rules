rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Public can read (for images, cv download)
      allow read: if true;
      // Only admin can upload
      allow write: if request.auth != null;
    }
  }
}
